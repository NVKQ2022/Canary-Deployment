apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.traffic.service.name }}
spec:
  selector:
    app: {{ .Values.app.name }}
  ports:
  - port: {{ .Values.traffic.service.port }}
    targetPort: {{ .Values.traffic.service.targetPort }}
