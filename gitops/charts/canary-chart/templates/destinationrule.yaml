apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ .Values.canary-traffic.destinationrule.name}}
spec:
  host: {{ .Values.canary-traffic.service.name }}
  subsets:
  - name: stable
    labels:
      version: stable
  - name: canary
    labels:
      version: canary